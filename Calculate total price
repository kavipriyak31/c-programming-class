#include <stdio.h>
int main() {
    float price, discount, total;
    printf("Enter price and discount %%: ");
    scanf("%f %f", &price, &discount);
    total = price - (price * discount / 100);
    printf("Final Price = %.2f", total);
    return 0;
}
