
#include <stdio.h>
#include <ctype.h>

int main() {
    char str[100];
    int i;

    printf("Enter string: ");
    scanf("%s", str);

    for (i = 0; str[i]; i++)
        str[i] = toupper(str[i]);

    printf("Uppercase: %s", str);
    return 0;
}
