#include <stdio.h>
int main() {
    int n,i,sum=0,m;
    printf("Enter number of students: ");
    scanf("%d",&n);
    for(i=1;i<=n;i++){
        printf("Enter marks: ");
        scanf("%d",&m);
        sum += m;
    }
    printf("Total marks = %d",sum);
    return 0;
}
