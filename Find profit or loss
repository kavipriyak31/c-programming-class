#include <stdio.h>
int main() {
    int cp, sp;
    printf("Enter cost price and selling price: ");
    scanf("%d %d",&cp,&sp);
    if(sp > cp)
        printf("Profit = %d", sp-cp);
    else
        printf("Loss = %d", cp-sp);
    return 0;
}
