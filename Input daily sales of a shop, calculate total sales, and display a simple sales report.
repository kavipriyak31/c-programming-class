#include <stdio.h>
#include <string.h>

int main() {
    char user[10], pass[10];
    scanf("%s %s", user, pass);

    if(strcmp(user,"admin")==0 && strcmp(pass,"123")==0)
        printf("Login Successful");
    else
        printf("Login Failed");
    return 0;
}
